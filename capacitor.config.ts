import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.utepsa.practicacamaragps',  
  appName: 'Proyecto App Movil',          
  webDir: 'www'
};

export default config;
